apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - agentgateway.yaml
  - kagent.yaml
  - agentregistry.yaml
  - kagent-default-model-config.yaml
  - discovery.yaml
  # Secrets are created from local .env with `make create-secrets` and are intentionally NOT committed here
